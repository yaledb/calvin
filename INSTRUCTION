1.  run `sudo install-ext` (change uname to your username at top)

2.  upload your id_rsa to ~/.ssh, chmod to 600 
    2.1 test whether you can ssh to other nodes, e.g., ssh node1

(every time you change protocol)
3.  run `clean_and_copy.sh` with appropriate options,
        DIR_SRC=~/calvin/src_traditional_2pl_2pc  OR DIR_SRC=~/calvin/src_calvin

4.  set the parameters in test_calvin.py in ___main____

5.   run `python3 test_calvin.py`